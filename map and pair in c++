#include<bits/stdc++.h>
using namespace std;
int main()
{
    /*pair<int,int>p={1,2};
    cout<<p.first<<" "<<p.second;
    cout<<endl;
    map<int,int>p1;
    p1.insert({1,8});
    p1.insert({4,6});
    p1.insert(pair<int,int>(1,2));
    p1.insert(pair<int,int>(1,3));
    map<int,int>::iterator it; we can use it for the iteration also;
    
    for(auto it=p1.begin();it!=p1.end();it++)
    {
        cout<<it->first<<" "<<it->second<<endl;
    }
    
    map<int,pair<int,int>>mp;
    for(int i=0;i<11;i++)
    {
        mp[i]={i+1,i+10};
    }
    
    //erasing the element in the map
    for(auto it=mp.begin();it!=mp.end();it++)
    {
        if(it->first==2)//erasing the value where the key value is 2
        {
            mp.erase(it->first);
        }
    }
    //second approh of approaching the elemnts
    mp.erase(4);//erasing the value where the key value is 4
    //-----------------------
    //mp.clear();  to clear the map all the values
    for(auto it=mp.begin();it!=mp.end();it++)
    {
        cout<<it->first<<" "<<it->second.first<<" "<<it->second.second<<endl;
    }
    cout<<mp.size();// to find out the size of the map
    */
    
    //-----------------------------------------------------------------------------------------
    pair<char,int>p1={'a',1};
    cout<<p1.first<<" "<<p1.second;
    
    
}
